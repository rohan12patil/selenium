{
  "name": "selenium",
  "version": "1.0.0",
  "description": "https://formy-project.herokuapp.com/",
  "main": "index.js",
  "scripts": {
    "test": "mocha tests/*.js --timeout 10000",
    "testS": "mocha tests/*.js  --timeout 10000 --reporter mochawesome",
    "testP": "mocha tests/*.js --timeout 10000 --parallel --reporter mochawesome  --require mochawesome/register",
    "test3": "mocha tests/thirdTest.js --timeout 10000",
    "test4": "mocha tests/fourthTest.js --timeout 10000",
    "wait": "mocha tests/wait.js --timeout 10000"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chai": "^5.2.0",
    "mochawesome": "^7.1.3",
    "selenium-webdriver": "^4.31.0"
  },
  "devDependencies": {
    "mocha": "^11.1.0"
  }
}
